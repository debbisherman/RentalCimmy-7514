---
deployment:
  tasks:
    # Space after '-' is REQUIRED for valid YAML
    - export DEPLOYPATH=/home/$(whoami)/public_html/
    
    # 1. Copy all built assets from dist to public_html
    - /bin/cp -R dist/* $DEPLOYPATH
    
    # 2. Ensure .htaccess is copied to the root
    - /bin/cp dist/.htaccess $DEPLOYPATH.htaccess
    
    # 3. Log deployment status
    - echo "Deployment completed at $(date)" >> $DEPLOYPATH/deploy_log.txt